# Local convenience wrapper around the production-shaped Compose file.
# Prefer: docker compose --profile local-db up --build
# Or:     docker compose -f docker-compose.prod.yml --profile local-db up --build

include:
  - path: docker-compose.prod.yml
